name: Tests

on: [push]

jobs:
  javascript:
    runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn
    - name: Run tests
      run: yarn test:ci
